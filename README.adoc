= soapui-testrunner-groovy (example)

This repo is contains example of local usage link:https://github.com/daggerok/soapui-testrunner-groovy[soapui-testrunner-groovy plugin]

[source,bash]
gradle testrunner
