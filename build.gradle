buildscript {
  repositories {
    jcenter()
    maven { url 'http://smartbearsoftware.com/repository/maven2/' }
  }
}

plugins {
  id 'io.github.daggerok.soapui-testrunner' version '0.0.1'
}

group 'daggerok'
version '0.0.1'

testrunner {
  projectFile "$projectDir/soapui-test-project.xml"
  outputFolder "$buildDir/tests"
}
